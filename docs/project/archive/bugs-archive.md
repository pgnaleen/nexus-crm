# Archived Bugs

Old entries moved out of `../BUGS.md`'s "Fixed / Closed" section once that list gets long enough
that scrolling past it gets in the way of the "Open" section. Recoverable via git history
regardless — this file exists so a *current* read of `BUGS.md` stays focused on what's still open
plus recent history. See `../README.md` for the archiving rule.

Empty for now — `BUGS.md`'s Fixed/Closed section is still short enough to read in place.
