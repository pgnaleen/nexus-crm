// Only the value Story 1.2 (Create a Task) actually produces today --
// Shared/Delegated/Accepted/InProgress/Completed/Archived get added here,
// each via its own migration, as Stories 1.5-1.10 are built.
export enum PriorityTaskStatus {
  Placed = "placed",
}
